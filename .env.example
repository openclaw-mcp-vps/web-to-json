NEXT_PUBLIC_STRIPE_PAYMENT_LINK=https://buy.stripe.com/test_XXX
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXX
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXX

# Optional but required for webhook signature verification and session entitlement writes.
STRIPE_SECRET_KEY=sk_test_XXXXXXXX

# Optional providers for AI-based extraction. If omitted, heuristic extraction is used.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# Optional cookie signing secret (falls back to STRIPE_WEBHOOK_SECRET when omitted).
ACCESS_COOKIE_SECRET=

# Optional public base URL used in metadata.
NEXT_PUBLIC_SITE_URL=https://webtojson.dev
